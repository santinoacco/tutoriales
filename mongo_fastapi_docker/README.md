# Mongo-FastAPI-Docker Infra tutorial

Follow the tutorial in [mongo_tutorial.ipynb](mongo_tutorial.ipynb).

Execute:
```bash
docker compose up -d
```
then open `http://localhost:8000/docs#/` in the browser.
